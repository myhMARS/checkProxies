        """ 单线程处理段
        with open("IP代理.txt", 'a') as f:
            logging.debug("打开文件")
            logging.debug(f'i:{len(ip)},p:{len(port)},ad:{len(address)},pr:{len(protocol)},an:{len(anonymity)}')
            for i, p, ad, pr, an in zip(ip, port, address, protocol, anonymity):
                n += 1
                print(f"检测第{n}个IP,目前可用{useful}个", end='', flush=False)
                print('\b' * (len(f"检测第{n}个IP,目前可用{useful}个") + 9), end='', flush=True)
                if check(i, p, headers, pr):
                    useful += 1
                    f.write("IP地址:" + '%-18s' % i + "端口:" + '%-6s' % p + "协议:" + '%-9s' % pr + "地址:" + '%-25s' % ad
                            + "匿名:" + '%-10s' % an + "\n")
            f.close()
        """